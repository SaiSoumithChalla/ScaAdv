import FebSeries._
object GetFunctions extends App {
  val x = "Reverse"
     for (i<-1 to 10){
      println(FebS(i))
}        // Getting FebSeries

   println(StringReverse(x))
   println(SecondaryStringReverse("Thoshiba"))
}
